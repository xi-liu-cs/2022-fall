unistd.h, sys/type.h, sys/wait.h
simulations value speed over convenience
special instruction int (asm) to translate system call
put a number in rax, then call int
if platform independent compiler, where is hardware
# hash is for preprocessor
after preprocessing, #include is replaced with code
#include <>, vs ""
<>: files exist in standard location, go to directory where gcc is installed
"": give relative path (current directory)
pid_t is defined in unistd
any process is an unique id which is nozero
child return to parent the id of newly created child's id
child itself return 0
https://man7.org/linux/man-pages/man2/nice.2.html
